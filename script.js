const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));const t=document.querySelector('.menu-toggle'),l=document.querySelector('.nav-links');t?.addEventListener('click',()=>l.classList.toggle('open'));l?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>l.classList.remove('open')));